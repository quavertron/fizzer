[package]
name = "alock-control"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
reqwest = { version = "=0.12.28", default-features = false, features = ["blocking", "rustls-tls"] }
tiny_http = "=0.12.0"
serde_json = "=1.0.151"

[profile.release]
panic = "abort"
